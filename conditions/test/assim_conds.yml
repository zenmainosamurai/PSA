### データ同化パラメータの初期値

# KFパラメータの初期値
KF_PARAMS: {
  p0: [0.00000032, 0.000626],      # 共分散行列の初期値
  Q: [0.0000002, 1.59],      # 状態遷移誤差
  R: [6.3151, 0.0781],     # 観測誤差
}

# 状態変数の初期値
STATE_VARS: {
#   "assim_coef_total_weights": 1, # 合計ゴミ重量の補正係数
  # "assim_coef_volatile_combustion": 1, # 揮発分放出量の補正係数
  "coef_heat_factor": 50, # 輻射熱の補正係数
  "shift_rate": 1, # 火格子速度の補正係数
}

# 観測変数
OBS_VARS: [
  # total_weights, # 合計ゴミ重量
  # idx_vol_max,   # 揮発分放出量最大位置
  idx_burn_out_vol,  # 燃え切り点
  gass_entropy,  # ガス発生量
]

# 中間変数
MID_VARS: [
  water_weights,
  volatile_weights,
  carbon_weights,
  ash_weights,
  garbage_temp,
  volatile_combustion,
  carbon_combustion,
]

# 中間変数の境界index
MID_VARS_INDEX: 140 # 火格子数×中間変数の数