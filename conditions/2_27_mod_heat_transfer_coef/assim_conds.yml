### データ同化パラメータの初期値

# KFパラメータの初期値
KF_PARAMS: {
  dt: 1, # timestmap
  p0: [0.05, 0.05],  # xの初期値・補正直後に対する分散
  Q: [0.0001, 0.0001],   # xの更新時に乗せるノイズの分散
  R: [0.005],   # zの更新時に乗せるノイズの分散
}

# 状態変数の初期値
STATE_VARS: {
  # "adsorp_cap": 0.9, # 吸着能係数
  "porosity": 0.3, # 空隙率
  "ptcl_size_ave": 0.9, # 平均粒子径
}

# 観測変数
OBS_VARS: [
  推定吸着量,
]