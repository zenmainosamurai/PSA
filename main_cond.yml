# main.pyを実行する際のパラメータ設定

# 実行モードを指定
mode_list: [
  # "simulation",
  # "assimilation",
  "optimize",
]

# 実行したいconditionファイルを指定
cond_list: [
  # 3_02_mod_desorp,
  # 3_02_use_obs_data,
  # 3_02_opt_params,
  3_03_mod_ks_desorp
]

# パラメータ最適化のパラメータ
opt_params: {
  num_processes: 6,
  num_trials: 100,
}