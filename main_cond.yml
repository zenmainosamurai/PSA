# main.pyを実行する際のパラメータ設定

# 実行モードを指定
mode_list: [
  "simulation",
  # "assimilation",
  # "optimize",
]

# 実行したいconditionファイルを指定
cond_list: [
  2_27_mod_heat_transfer_coef
]

# パラメータ最適化のパラメータ
opt_params: {
  num_processes: 8,
  num_trials: 100,
}