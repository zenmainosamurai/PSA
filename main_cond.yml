# main.pyを実行する際のパラメータ設定

# 実行モードを指定
mode_list: [
    simulation,
    # assimilation,
    # optimize,
]

# 実行したいconditionファイルを指定
cond_list: [
  # 5_01_mod_conds_adjust,
  5_01_mod_conds_yaml2xlsx
  # test
]

# パラメータ最適化のパラメータ
opt_params: {
  num_processes: 4,
  num_trials: 100,
}