# main.pyを実行する際のパラメータ設定

# 実行モードを指定
mode_list: [
    # simulation,
    assimilation,
    # optimize,
]

# 実行したいconditionファイルを指定
cond_list: [
    test_assim2
]

# パラメータ最適化のパラメータ
opt_params: {
  num_processes: 6,
  num_trials: 100,
}