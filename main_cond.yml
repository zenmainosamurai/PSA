# main.pyを実行する際のパラメータ設定

# 実行モードを指定
mode_list: [
  "simulation",
  # "assimilation",
  # "optimize",
]

# 実行したいconditionファイルを指定
cond_list: [
  # 4_06_mod_manual_mode,
  test
]

# パラメータ最適化のパラメータ
opt_params: {
  num_processes: 6,
  num_trials: 100,
}